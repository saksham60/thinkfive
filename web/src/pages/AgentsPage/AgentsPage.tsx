const agents=['Supervisor','Banking','Fraud','Knowledge','Case','Synthesis'];export function AgentsPage(){return <div className="page"><header><p className="eyebrow">AGENT OPERATIONS</p><h1>Coordinated specialists</h1><p>Runtime details will populate from backend capability and trace endpoints.</p></header><div className="grid">{agents.map((name,i)=><article className="card agent" key={name}><span>{String(i+1).padStart(2,'0')}</span><h2>{name}</h2><p>{name==='Supervisor'?'Routes work and governs the run.':'Contributes typed evidence to the supervised workflow.'}</p></article>)}</div></div>}
