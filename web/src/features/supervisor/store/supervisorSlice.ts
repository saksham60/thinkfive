import{createSlice}from'@reduxjs/toolkit';import type{SupervisorMetrics}from'../types/supervisor.types';import{fetchSupervisorMetrics}from'./supervisorThunks';const s=createSlice({name:'supervisor',initialState:{metrics:null as SupervisorMetrics|null,status:'idle' as'idle'|'loading'|'succeeded'|'failed'},reducers:{},extraReducers:b=>{b.addCase(fetchSupervisorMetrics.pending,x=>{x.status='loading'}).addCase(fetchSupervisorMetrics.fulfilled,(x,a)=>{x.metrics=a.payload;x.status='succeeded'}).addCase(fetchSupervisorMetrics.rejected,x=>{x.status='failed'})}});export default s.reducer;
