import{createSlice,type PayloadAction}from'@reduxjs/toolkit';export type ConnectionState='connecting'|'live'|'reconnecting'|'offline';const s=createSlice({name:'system',initialState:{connection:'offline'as ConnectionState},reducers:{connectionChanged:(x,a:PayloadAction<ConnectionState>)=>{x.connection=a.payload}}});export const{connectionChanged}=s.actions;export default s.reducer;
