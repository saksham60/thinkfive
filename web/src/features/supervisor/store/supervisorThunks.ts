import{createAsyncThunk}from'@reduxjs/toolkit';import{apiRequest}from'@/api/client';import{endpoints}from'@/api/endpoints';import type{SupervisorMetrics}from'../types/supervisor.types';export const fetchSupervisorMetrics=createAsyncThunk('supervisor/metrics',()=>apiRequest<SupervisorMetrics>(endpoints.supervisor.metrics));export const triggerSimulator=createAsyncThunk('supervisor/simulator',(scenarioType:string)=>apiRequest<unknown>(endpoints.supervisor.simulator,{method:'POST',body:{scenarioType}}));
