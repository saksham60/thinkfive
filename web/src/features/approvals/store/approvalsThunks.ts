import{createAsyncThunk}from'@reduxjs/toolkit';import{apiRequest}from'@/api/client';import{endpoints}from'@/api/endpoints';import type{Approval}from'../types/approval.types';
export const fetchApprovals=createAsyncThunk('approvals/fetch',()=>apiRequest<Approval[]>(endpoints.approvals));export const approveAction=createAsyncThunk('approvals/approve',(id:string)=>apiRequest<Approval>(`${endpoints.approvals}/${encodeURIComponent(id)}/approve`,{method:'POST'}));export const rejectAction=createAsyncThunk('approvals/reject',(id:string)=>apiRequest<Approval>(`${endpoints.approvals}/${encodeURIComponent(id)}/reject`,{method:'POST'}));
